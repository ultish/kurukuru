# Verification — {{ID}} {{TITLE}}

_Verifier: <agent/name> · {{DATE}}_

> Written by an agent that did NOT build this slice. Every PASS below cites a fact
> the verifier observed — not a restatement of the criterion, not the builder's
> claim.

## Gate run
<!-- Paste the `keel gate {{ID}}` summary line and date. Green gates are necessary,
not sufficient. -->

## Per-criterion findings
| AC | Verdict | Evidence (observed fact) |
|----|---------|--------------------------|
| AC-1 | PASS / FAIL | <test output / real HTTP response / screenshot / log line> |
| AC-2 | PASS / FAIL | <...> |

## Out-of-contract bugs found
<!-- Granular, actionable findings beyond the acceptance criteria, e.g.
"fillRectangle exists but never fires on mouseUp". Empty is fine. -->

## Verdict
**verified** / **rejected** — <one-line rationale>

<!-- If rejected: list exactly what must change, specific enough that the builder
can act without re-reading this whole report. -->
